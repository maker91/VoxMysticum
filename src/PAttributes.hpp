#pragma once


struct PAttributes
{
    // magic attributes
    float shootDelay = 0.32f;
    float projectileSpeed = 500.f;
    int damage = 25;

    // player attributes
    float maxSpeed = 600.f;
    int maxHealth = 6;
};